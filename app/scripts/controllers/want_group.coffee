dont.controller "WantGroupCtrl", ($scope) ->
  $scope.drawGroup = ()->
    # s = Snap("#svg")

    # group = s.marker(0, 0, '50%', '50%')
    # group_circle = paper.circle('100%', '100%', '100%')
    # group_mask = paper.rect(0, 0, '100%', '100%')
    # group_mask.attr {fill: "#fff"}
    # group_circle.attr {mask: group_mask}

    # #s.add(group) #group.clone().attr({cx: '100', fill: "#f00"})

    # setTimeout () ->
    #   group.animate({r: '25%'}, 1000, mina.elastic)
    # , 1000